[package]
name = "iris"
version = "0.1.0"
edition = "2024"

[dependencies]
crossbeam-channel = "0.5.15"
foldhash = "0.2.0"
oneshot = { version = "0.2.1", features = ["std"] }

[dev-dependencies]
divan = "0.1.21"

[[bench]]
name = "operations"
harness = false